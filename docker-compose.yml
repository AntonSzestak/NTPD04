version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    networks:
      - appnet

  redis:
    image: redis
    ports:
      - "6379:6379"
    networks:
      - appnet

networks:
  appnet:
